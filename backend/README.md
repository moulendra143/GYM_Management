Backend scaffold (ASP.NET Core Web API)

Quick start:

1. Install .NET SDK 8.0 or compatible.
2. Open a terminal in `backend/` and run:

```bash
dotnet restore
dotnet run
```

The API uses SQLite (`gym.db`) and creates the DB automatically. Update `appsettings.json` for JWT and connection string.
